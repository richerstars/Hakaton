import styled from 'styled-components';
import {COLORS} from "../../../constants/colors";
// import {COLORS} from "../../constants/colors";

export const StForm = styled.form`
  display: block;
  padding: 45px;
  background-color: ${COLORS.PRIMARY_COLOR_LIGHT};
  text-align: center;
  width: 450px;
  margin: 0 auto;
  color: ${COLORS.WHITE};
  border-radius: 20px;
`;

export const StP = styled.form`
    font-size: 30px;
`;
